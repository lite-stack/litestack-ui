export default class ServerCreate {
    constructor(name, description, configuration_name) {
        this.name = name;
        this.description = description;
        this.configuration_name = configuration_name;
    }
}